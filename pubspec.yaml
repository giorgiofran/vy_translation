name: vy_translation
description: A way to manage message translations even from other packages (that use vy_translation ...)
version: 0.2.0
# homepage: https://www.example.com
author: giorgio <giorgiofran@gmail.com>

environment:
  sdk: '>=2.5.0 <3.0.0'

dependencies:
  yaml: any
  xml: any
  recase: any
  vy_lock: #^1.6.0
    git:
      url: "https://github.com/giorgiofran/vy_lock"
    #path: ../../git/vy_lock

  vy_language_tag:
    git:
      url: "https://github.com/giorgiofran/vy_language_tag"
    #path: ../../git/vy_language_tag

dev_dependencies:
  pedantic: 1.8.0
  test: ^1.6.0
